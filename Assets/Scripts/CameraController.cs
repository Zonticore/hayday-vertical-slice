using System;

public class CameraController : MonoBehaviourSingleton<CameraController>
{
    //if the context menu is open center the tile of the context menu.
    //If no context menu or ui screen is open allow dragging to move the camera around.
    
}